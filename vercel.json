{
  "buildCommand": "cd frontend && npm install --cache /tmp/npm-cache && npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index.py" }
  ]
}
